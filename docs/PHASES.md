# PHASES.md — EPS Build Task Tracker (repo-canonical)
Claude Code: work ONE task at a time, top-down, within the current phase. Update your task's row (Status, SHA, Notes) in the same commit as the work. Do not begin the next phase until the current phase's gate PR is approved by a human. Statuses: NOT STARTED / IN PROGRESS / BLOCKED / DONE.

**Current phase: P0**

## P0 — Scaffold — GATE: CI fully green on hello-world; secret-scan + mainnet-grep active
| ID | Task | Acceptance criteria | Status | SHA | Notes |
|---|---|---|---|---|---|
| T-001 | Repo hygiene: .gitignore, .github/ (CI workflow, claude workflow, phase-gate PR template), devcontainer.json | Devcontainer pins Node 20, pnpm, Solana CLI, Stripe CLI; PR template has gate checklist | DONE | _pending_ | Added `.devcontainer/` (Node 20 base + pnpm/Solana/Stripe CLI via post-create) and `.github/pull_request_template.md` with gate checklist. `.gitignore` already covers node/env/secrets. CI workflow now in place at `.github/workflows/ci.yml` (gitleaks + mainnet-grep + lint/typecheck/unit). |
| T-002 | Next.js 15 + TS strict + Tailwind v4 scaffold | `pnpm dev` serves; `tsc --noEmit` clean | DONE | d4e37cb | Next.js 15.5 App Router (no `src/`, `app/` at root) + React 19. TS `strict: true`. Tailwind v4 via `@tailwindcss/postcss` + `@import "tailwindcss"` in `app/globals.css` (no `tailwind.config.js`). ESLint 9 flat config extending `next/core-web-vitals` + `next/typescript`. Hello-world `app/page.tsx`. Added vitest + smoke test so CI `pnpm test` is green (full suite in T-005). Verified locally: `typecheck`, `lint`, `test`, `build` all exit 0. |
| T-003 | docker compose: Postgres 16 + MinIO; Prisma init + seed | `pnpm db:up && pnpm db:migrate` succeed; seed runs | DONE | _pending_ | `docker-compose.yml`: `postgres:16-alpine` + `minio/minio` with healthchecks (`pg_isready` / `mc ready`) and named volumes. `prisma/schema.prisma` postgresql datasource + placeholder `Health` model; initial migration `20260612193123_init` committed. Scripts `db:up`/`db:down`/`db:migrate`/`db:seed` added; `db:seed` runs via `prisma db seed` → `tsx prisma/seed.ts` logging "seed ok". `DATABASE_URL` in `.env.example`. Verified locally end-to-end: `db:up` (both healthy) → `db:migrate` (applied) → `db:seed` (clean); `typecheck`/`lint`/`test` green. |
| T-004 | Clerk integration (email verify + 2FA) + server-side token helper | Protected route rejects missing/invalid token; userId/orgId derived server-side only | DONE | _pending_ | Added `@clerk/nextjs` 7.5.2. `app/layout.tsx` wrapped in `<ClerkProvider>`. `middleware.ts` runs `clerkMiddleware`, calling `auth.protect()` on `/dashboard(.*)` so unauthenticated requests are rejected at the edge. `lib/auth.ts` exposes `requireAuth()` → `{ userId, orgId }` derived from the verified session token via `auth()`; throws `UnauthorizedError` when no user or no active org (never reads client input). `app/dashboard/page.tsx` is a server component calling the helper. Clerk env vars added to `.env.example` (test keys only). Unit tests in `test/auth.test.ts` cover unauthenticated / no-org / success. `typecheck`, `lint`, `test` all green. Email verify + 2FA are enabled in the Clerk dev instance dashboard (instance config, not code). |
| T-005 | CI: lint, typecheck, unit, integration (solana-test-validator service), e2e, gitleaks, mainnet-grep | All jobs green on PR | DONE | _pending_ | CI split into jobs: `secret-scan` (gitleaks) + `mainnet-grep` (kept), `lint` (`pnpm lint`), `typecheck` (`pnpm tsc --noEmit`), `unit` (`pnpm test -- --run`), `integration` (solana-test-validator service on :8899 + `pnpm test:integration`), `e2e` (echo placeholder until P5/T-502). Added `vitest.config.ts` (unit; excludes `tests/integration`) and `vitest.integration.config.ts` (integration-only). Scripts: `test` → `vitest`, `test:integration` → `vitest run --config vitest.integration.config.ts`. `test/smoke.test.ts` + new `tests/integration/smoke.integration.test.ts` pass. Also ignored generated `next-env.d.ts` in eslint (its triple-slash ref tripped `next/typescript`). Verified locally: lint, typecheck, unit (4), integration (2) all green. |

## P1 — Subscriptions & quotas — GATE: tier quota correct on dashboard; webhook replay-safe; comp grant works
| ID | Task | Acceptance criteria | Status | SHA | Notes |
|---|---|---|---|---|---|
| T-101 | Prisma: Organization, Subscription, WebhookEvent | Migration applies; relations enforced | DONE | _pending_ | Added `Organization` (id, `clerkOrgId` @unique, name, createdAt), `Subscription` (id, `orgId` FK → Organization with `onDelete: Cascade` + `@@index`, tierId, `status` enum `SubscriptionStatus` ACTIVE/CANCELED/PAST_DUE, periodStart, periodEnd, `usageCount` Int @default(0)), and `WebhookEvent` (id, `eventId` @unique, processedAt) to `prisma/schema.prisma`. Migration `20260612200900_add_org_sub_webhook` created. Verified locally against `postgres:16-alpine`: `prisma validate` clean, `migrate deploy` applies both migrations, and `migrate diff --from-migrations --to-schema-datamodel` reports "No difference detected" (no drift). |
| T-102 | scripts/stripe-bootstrap.ts (Tier1 $200 / Tier2 $600 / Tier3 $1000 + EARLYADOPTER50, test mode) | Idempotent re-run; price IDs documented in .env.example | DONE | _pending_ | Added `scripts/stripe-bootstrap.ts` (stripe-node 22.x). Creates Tier1/2/3 products + monthly recurring prices (20000/60000/100000 cents USD) and the `EARLYADOPTER50` coupon (50% off, `repeating` 12 months). Idempotency via `metadata.bootstrapKey`: products looked up with `products.search`, prices via active-price scan on the product, coupon via list-scan — re-runs reuse existing objects instead of duplicating. `assertTestMode()` aborts on `sk_live_`/`rk_live_` keys (CLAUDE.md hard rule #7). Prints `STRIPE_TIER{1,2,3}_PRICE_ID` for `.env`; placeholders added to `.env.example` alongside `STRIPE_SECRET_KEY`. `lint` + `typecheck` green. |
| T-103 | Subscription checkout + promo code field | Subscription-mode session; promo applies 50% | DONE | _pending_ | Added `lib/stripe.ts` (lazy shared Stripe client with a live-key guard per hard rule #7; `Tier` type + `isTier` guard + `priceIdForTier` env resolver). `app/api/checkout/route.ts`: `POST`, auth-required via `requireAuth()` (401 on `UnauthorizedError`), validates `tier` from the JSON body (400 on bad/missing tier or malformed JSON), then creates a Stripe Checkout session with `mode: "subscription"` + `allow_promotion_codes: true` (so the hosted page shows the promo field — EARLYADOPTER50 → 50% off). Org/user come from the verified token only; stamped onto session + `subscription_data` metadata and `client_reference_id` for the T-104 webhook. `app/pricing/page.tsx`: client component with 3 tier cards (Tier1 $200 / Tier2 $600 / Tier3 $1000, quotas 1/9/999) and Subscribe buttons that POST to `/api/checkout` and redirect to `session.url`; facilitation-safe copy. Added `@`→root alias to `vitest.config.ts` so `@/...` imports resolve under vitest. Tests: `test/stripe.test.ts` (tier guard, price resolver, live-key/no-key guards) + `test/checkout-route.test.ts` (401 unauth, 400 bad tier / bad JSON, happy path asserts subscription mode + promo flag + token-derived org). `lint`, `typecheck`, `test` (14) all green. |
| T-104 | Webhook handler: signature verify + event-id dedupe | Replayed event is a no-op (test asserts) | DONE | _pending_ | `app/api/webhooks/stripe/route.ts` (`POST`, `dynamic = "force-dynamic"`): reads the raw body via `req.text()`, requires the `Stripe-Signature` header (400 if missing), and verifies it with `stripe.webhooks.constructEvent(payload, sig, STRIPE_WEBHOOK_SECRET)` — bad/absent signature → 400, no DB write; missing `STRIPE_WEBHOOK_SECRET` → 500 (fail loud, never accept unverifiable events). Idempotency: inside one `prisma.$transaction`, `webhookEvent.create({ data: { eventId } })` runs FIRST as the dedupe guard — a replay hits the `eventId` unique constraint (P2002), the whole tx rolls back, and the route returns 200 `{ deduped: true }` with no side effects. On `checkout.session.completed` it upserts the `Organization` (by `clerkOrgId` from the token-stamped `client_reference_id`/metadata) then upserts `Subscription` → ACTIVE, keyed by the Stripe subscription id so it is itself idempotent (exact period boundaries reconciled in T-105). Added `lib/db.ts` (global-cached `PrismaClient` singleton). Unit test `tests/unit/webhook-dedupe.test.ts`: missing-sig 400, bad-sig 400, happy-path ACTIVE upsert, and the replay assertion (2nd delivery → 200, `subscription.upsert` called exactly once). `STRIPE_WEBHOOK_SECRET` added to `.env.example`. `lint`, `typecheck`, `test` all green. |
| T-105 | Quota meter + enforcement service | Tier1=1, Tier2=9, Tier3=999 per period; resets on rollover | DONE | _pending_ | Added `lib/quota.ts`: `TIER_QUOTA` map (`tier1`=1, `tier2`=9, `tier3`=999, keyed by the `Subscription.tierId` wire values the checkout/webhook stamp), `QuotaExceededError` + `NoActiveSubscriptionError`, and `checkAndDecrementQuota(orgId)`. It looks up the org's ACTIVE `Subscription` (by the verified Clerk `orgId` via the `organization` relation, latest period first). On rollover (`now > periodEnd`) it resets `usageCount` to 0 and opens a fresh one-month period, so the call counts as the new period's first unit (`usageCount` → 1). Otherwise enforcement is an **atomic** conditional increment — `updateMany({ where: { id, usageCount: { lt: limit } }, data: { usageCount: { increment: 1 } } })` — so concurrent callers can never overshoot the tier limit; `count === 0` (guard failed) → `QuotaExceededError`, nothing mutated. Unit tests `tests/unit/quota.test.ts`: under-limit passes & increments with the guarded `where`, at-limit (tier1, usage 1) throws `QuotaExceededError` with no reset, elapsed-period (at limit, `periodEnd` in past) resets & passes asserting the `update` reset + advanced `periodEnd`, and no-active-subscription throws. `lint`, `typecheck`, `test` (22) all green. |
| T-106 | Comp-grant admin route (Founders Legacy, 24mo) | Admin-only; AuditLog row written | DONE | _pending_ | Added `AuditLog` model (`id`, `action`, `actorId`, `targetId`, `metadata Json?`, `createdAt @default(now())`) + migration `20260612205400_add_audit_log`. `lib/auth.ts` gains `ForbiddenError`, `ORG_ADMIN_ROLE`, and `requireOrgAdmin()` — reads the `org:admin` role from the verified Clerk token via `has({ role })` (never client input); 401 unauth / no-org, 403 non-admin. `app/api/admin/comp-grant/route.ts` (`POST`): admin-only, validates `{ targetOrgId }` (400 on missing/bad/non-JSON), then in ONE `prisma.$transaction` upserts the target `Organization` (by `clerkOrgId`), reconciles its `Subscription` → Tier3/ACTIVE with a 24-month period (`usageCount` reset to 0), and writes a `COMP_GRANT` `AuditLog` row (CLAUDE.md hard rule #5). Unit test `test/comp-grant-route.test.ts`: non-admin → 403 (no tx), plus 401 unauth, 400 missing/malformed body, happy-path grant, and a tx-double assertion that Tier3/ACTIVE create + COMP_GRANT audit write happen together. `lint`, `typecheck`, `test` (28) all green. |

## P2 — Service intake — GATE: submission yields STAGED record with encrypted object + notice token; bad input rejected pre-quota
| ID | Task | Acceptance criteria | Status | SHA | Notes |
|---|---|---|---|---|---|
| T-201 | Upload endpoint: MIME + magic bytes + 25MB cap | Spoofed-extension file rejected | DONE | _pending_ | Added `app/api/upload/route.ts` (`POST`, `runtime = "nodejs"` since file-type sniffs raw bytes). Parses `multipart/form-data` (400 if not multipart / no `file` field), enforces the 25 MB cap via `file.size` **before** buffering (`MAX_UPLOAD_BYTES = 25*1024*1024` → 413 on overflow), checks the declared `Content-Type` against the document whitelist `[application/pdf, application/msword, application/vnd.openxmlformats-officedocument.wordprocessingml.document]` (415 otherwise), then verifies magic bytes with `fileTypeFromBuffer` (file-type 22.0.1): the sniffed signature must be consistent with the declared MIME (`application/pdf`; `.doc`→`application/x-cfb`; `.docx`→OOXML mime or `application/zip`), so a spoofed-extension file is rejected (415). No document bytes are logged (hard rule #3); validation only — encrypt/persist is T-204/T-205. Unit test `test/upload-route.test.ts`: valid PDF passes (200), 26 MB PDF rejected on size (413), `.pdf` extension with PNG magic bytes rejected (415). `lint`, `typecheck`, `test` (31) all green. |
| T-202 | Solana address validation (base58 + on-curve) | PDA/off-curve rejected with clear error | DONE | _pending_ | Added `@solana/web3.js` and `lib/solana/validate-address.ts`. `assertValidRecipient(addr)` parses the string via `new PublicKey(addr)` (throws `InvalidRecipientError` with `reason: "malformed"` on a non-base58 / wrong-length string) then rejects off-curve keys via `PublicKey.isOnCurve(pubkey.toBytes())` (throws `reason: "off-curve"`) so PDAs — which have no private key and can't hold a signable balance — are rejected with a descriptive message; returns the parsed `PublicKey` on success. Unit test `test/validate-address.test.ts`: a generated wallet passes, a runtime-derived PDA (`findProgramAddressSync`, asserted off-curve) is rejected with reason `off-curve`, and a random string / empty / wrong-length base58 are rejected with reason `malformed`. `lint`, `typecheck`, `test` (36) all green. |
| T-203 | Case-caption form + attestation + optional court-order flag | Required-field validation; values persisted | DONE | _pending_ | Added `ServiceRequest` model (`id`, `orgId` FK → Organization `onDelete: Cascade` + `@@index`, `caseCaption`, `plaintiffName`, `defendantName`, `recipientWallet`, `courtOrderFlag` Bool @default(false), `attestedAt`, `noticeToken String? @unique`, `status ServiceStatus @default(STAGED)`, `txSignature?`, `slot BigInt?`, `blockTime?`, `createdAt`) + `ServiceStatus` enum (STAGED/IN_PROGRESS/CONFIRMED/FAILED); migration `20260612212002_add_service_request` (verified via `prisma migrate dev` against `postgres:16-alpine`). `app/api/service-requests/route.ts` (`POST`): `requireAuth()` (401), then **zod** body validation (400 with per-field `issues`) → on-curve recipient check via `assertValidRecipient` (400, `reason`) → `checkAndDecrementQuota(orgId)` (403 exhausted / 402 no-sub) → `prisma.serviceRequest.create` STAGED — all validation happens **pre-quota** (P2 gate). Org resolved from the verified token (`connect` by `clerkOrgId`), `attestedAt` stamped server-side; caption fields never logged (hard rule #3). `app/dashboard/new/page.tsx`: client form with all required fields + recipient wallet + optional court-order toggle + required attestation checkbox (submit disabled until attested); facilitation-safe copy (hard rule #6); surfaces server field errors. New dep `zod` ^4 introduced under `docs/adr/001-zod-for-server-side-input-validation.md` (CLAUDE.md no-new-deps-without-ADR). Test `test/service-requests-route.test.ts` (9): happy-path STAGED + quota decrement, 401 unauth, missing-attestation / blank-field / off-curve PDA / malformed-address / bad-JSON all rejected pre-quota, 403 quota-exceeded, 402 no-subscription. `lint`, `typecheck`, `test` (45) all green. NOTE: quota-decrement + STAGED create are not yet in one tx — that (with AuditLog + rollback-restores-quota) is T-205. |
| T-204 | SHA-256 + AES-256-GCM encrypt + private object put | Hash stable on re-read; object not publicly readable | DONE | _pending_ | Added `@aws-sdk/client-s3` (MinIO is S3-compatible). `lib/storage.ts`: `storeDocument(buf: Buffer)` computes the plaintext SHA-256 (hex, stable on re-read), AES-256-GCM encrypts with a fresh random 12-byte IV using the 32-byte key from `STORAGE_ENCRYPTION_KEY` (accepts hex-64 or base64; fails loud if unset/wrong length — never a weak fallback), and PUTs the **ciphertext** to bucket `eps-documents` (`MINIO_BUCKET`) under `documents/<uuid>` with **no public ACL** (object stays private under the bucket default). Returns key references only `{ objectKey, sha256, iv, authTag }` — plaintext + key never leave memory, no document bytes logged (hard rule #3). Lazy `S3Client` (`forcePathStyle: true` for MinIO) from `MINIO_ENDPOINT`/`MINIO_ACCESS_KEY`/`MINIO_SECRET_KEY`. Also exports `decryptDocument()` (GCM auth-tag verified → tamper throws) for re-read/round-trip. Wired into `app/api/upload/route.ts` (T-201): after validation passes it encrypts+persists and returns `objectKey`/`sha256`/`iv`/`authTag`. Env vars added to `.env.example` (`STORAGE_ENCRYPTION_KEY`, `MINIO_ENDPOINT`, `MINIO_ACCESS_KEY`, `MINIO_SECRET_KEY`, `MINIO_BUCKET`). Tests: `test/storage.test.ts` (S3 client mocked) — storeDocument→decrypt round-trip yields identical bytes, asserts private PUT (correct bucket/key, no ACL) of ciphertext≠plaintext, plaintext-SHA stability, IV/tag shapes, tampered-ciphertext rejection, and missing/short-key throws; `test/upload-route.test.ts` stubs `storeDocument` and asserts the route returns the storage metadata. `lint`, `typecheck`, `test` (48) all green. |
| T-205 | Notice token (128-bit) + Service STAGED + AuditLog + quota decrement in ONE transaction | Rollback restores quota | DONE | _pending_ | Added `lib/intake.ts` `createServiceRequest(input)`: a single `prisma.$transaction` that (1) consumes one unit of quota via `checkAndDecrementQuota(orgId, tx)` (atomic guard + rollover, now tx-aware), (2) encrypts + privately stores the document (`storeDocument`), (3) creates the `ServiceRequest` STAGED with a fresh 128-bit notice token (`crypto.randomBytes(16).toString("hex")` → 32 hex chars, unique), and (4) writes a `SERVICE_REQUEST_STAGED` `AuditLog` row in the SAME tx (hard rule #5) — all-or-nothing. Refactored `lib/quota.ts` `checkAndDecrementQuota` to accept an optional `QuotaDbClient` (defaults to shared `prisma`) so the decrement participates in the intake tx and rolls back with it. Storage runs INSIDE the tx (after the decrement) so a storage failure aborts the whole intake: "rollback restores quota". Caption/document bytes never logged (hard rule #3); audit metadata carries only key refs (objectKey, plaintext sha256). Integration test `tests/integration/intake.integration.test.ts`: mocks `storeDocument` to throw mid-transaction and drives the REAL `createServiceRequest`/`checkAndDecrementQuota` against an in-memory Prisma fake with genuine `$transaction` rollback (commit clone on success, discard on throw) — asserts `usageCount` unchanged and zero `ServiceRequest`/`AuditLog` rows on failure, plus a happy-path that commits decrement+STAGED+audit together and validates the 128-bit token. Added the `@`→root alias to `vitest.integration.config.ts` so `@/...` resolves in the integration suite. No schema change (uses existing `noticeToken`/`AuditLog`). `lint`, `typecheck`, unit (48), integration (4) all green. NOTE: wiring the JSON `/api/service-requests` route (which has no file) onto this unified intake primitive is a follow-up — `createServiceRequest` is the canonical atomic intake unit the upload↔intake merge will call. |

## P3 — Chain delivery — GATE: integration suite green on local validator incl. retry tests; ONE manual devnet delivery verified in explorer
| ID | Task | Acceptance criteria | Status | SHA | Notes |
|---|---|---|---|---|---|
| T-301 | ChainAdapter interface + Solana impl + assertNotMainnet | Constructor throws on mainnet RPC (unit test) | DONE | _pending_ | `lib/chain/types.ts` defines the `ChainAdapter` seam: `assertNotMainnet(): void` + `deliver({recipientWallet, lamports, memoParts}) → {signature, slot, blockTime}` (`ChainDeliveryResult`), so the rest of the app never imports `@solana/web3.js` directly (CLAUDE.md "all chain calls behind lib/chain"). `lib/chain/solana.ts` `SolanaAdapter implements ChainAdapter`: constructor takes `(rpcUrl, signer: Keypair)` and calls `assertNotMainnet()` BEFORE opening the `Connection` (commitment `finalized`) — a substring "mainnet" match (case-insensitive, catches public `api.mainnet-beta` AND private mainnet RPCs) throws `Error("Mainnet RPC forbidden")` (hard rule #2). `deliver()` validates the recipient via existing `assertValidRecipient` (rejects off-curve/PDA before sending), builds `SystemProgram.transfer` + `createMemoInstruction(memoParts.join(" \| "))` from `@solana/spl-memo` (added — already in the approved stack; real Memo program id `MemoSq4gqABAXKb96qnH8TysNcWxMyWCqXgDLGmfcHr`, not hardcoded), `sendAndConfirmTransaction` at `finalized`, then re-reads via `getTransaction` for the authoritative slot/blockTime. `getSolanaAdapter()` factory reads `SOLANA_RPC_URL` + `SOLANA_SIGNER_KEYPAIR` (base58, decoded with `bs58` — added as direct dep) and fails loud if unset (no weak fallback); signer secret comes from env only, never logged/persisted (hard rule #1). `lib/chain/index.ts` re-exports the interface + factory. Unit test `__tests__/chain/solana.test.ts` (Connection + `sendAndConfirmTransaction` mocked): constructor + `assertNotMainnet()` throw on public/private/UPPERCASE mainnet URLs and do NOT throw on devnet/localhost; `deliver()` builds exactly transfer+memo and returns signature/slot/blockTime; off-curve recipient rejected before send; factory throws on missing env, refuses mainnet, builds from valid devnet env. `.env.example` documents `SOLANA_RPC_URL`/`SOLANA_SIGNER_KEYPAIR`. `lint`, `typecheck`, unit (59) all green. NOTE: the full memo field formatting (`sha256:\|notice:\|svc:`) + rent-exempt-min sourcing is T-302; the worker's persist-sig-before-confirm loop is T-303. |
| T-302 | Tx builder: transfer(rent-exempt min) + Memo(sha256\|notice\|svc) | Memo decodes to expected fields | DONE | _pending_ | `lib/chain/tx-builder.ts` exports `buildServiceTx({connection, fromKeypair, recipientPubkey, sha256, noticeToken, serviceId}) → Promise<Transaction>`: builds (but does NOT send) the v1 anchor — `SystemProgram.transfer` of `await connection.getMinimumBalanceForRentExemption(0)` (rent-exempt min for a 0-data account) + `createMemoInstruction` (`@solana/spl-memo`) carrying `${sha256}\|${noticeToken}\|${serviceId}`, with `feePayer` set to the app wallet. Off-curve recipients (PDAs, no signer) are rejected via `PublicKey.isOnCurve` BEFORE the rent read. Sending/persist-sig-before-confirm at `finalized` stays the worker's job (T-303). Unit test `__tests__/chain/tx-builder.test.ts` (mock `Connection`, no socket): locates the memo ix by `MEMO_PROGRAM_ID`, UTF-8 decodes `ix.data`, asserts all 3 pipe-separated fields match; asserts the transfer ix carries exactly the rent-exempt lamports (`SystemInstruction.decodeTransfer`) and `getMinimumBalanceForRentExemption(0)`; off-curve/PDA recipient rejected before any rent read. `lint`, `typecheck`, unit (62) all green. |
| T-303 | Worker: DB-polled loop + drain-and-exit mode (`worker:once`) | Crash mid-job resumes idempotently | DONE | _pending_ | Added `worker/index.ts` (the DB-polled fulfilment loop; the queue is the `ServiceRequest` table, no Redis). `claimNext()` atomically claims the oldest claimable row via find-then-conditional-`updateMany` (`where status in [STAGED, IN_PROGRESS]` → set IN_PROGRESS): the guarded `updateMany` IS the lock — `count === 0` means a concurrent poller already took it, so two workers can never both win a row. Crucially the claim ALSO re-selects rows already IN_PROGRESS — in this single-worker model an IN_PROGRESS row at poll time is one a crashed worker left mid-job, so re-claiming RESUMES it ("crash mid-job resumes idempotently"). `runOnce()` claims+processes one job to a terminal state; a caught delivery error parks the row FAILED (terminal, so `drain` can't spin — full failure path w/ quota restore is T-306); an uncaught crash leaves it IN_PROGRESS for the next run. `drain()` (the body of `worker:once`) processes the whole backlog then the entry point `exit(0)`s after `prisma.$disconnect()`; the long-running mode (`pnpm worker`) polls one job per 5s tick forever and never lets an infra blip kill the loop. Auto-run guarded by an `import.meta.url === argv[1]` check so importing the module in tests doesn't start the loop. `worker/process.ts` is the default delivery: sends the rent-exempt transfer + Memo through the `ChainAdapter` seam (`getSolanaAdapter()` + new in-seam `getRentExemptMinimum()` so the worker never imports `@solana/web3.js` directly; mainnet guard refactored into shared `assertNotMainnetUrl()`), then stamps txSignature/slot/blockTime → CONFIRMED — persist-sig-BEFORE-confirm (rule #4) + re-read verification + memo `sha256:` sourcing are the explicit T-304/T-305 follow-ups. Scripts: `worker` / `worker:once` (`DRAIN_AND_EXIT=1`). No caption/document bytes logged (rule #3); only ids/statuses. Fault test `tests/integration/worker.integration.test.ts` drives the REAL `drain`/`runOnce`/`claimNext` against an in-memory table with genuine atomic-claim semantics: a row left IN_PROGRESS is re-claimed and delivered with the send spy called EXACTLY once; plus STAGED→CONFIRMED happy path, terminal-row no-double-send, full-backlog drain in createdAt order, delivery-throws→FAILED, lost-race claim (`count === 0`), empty-queue. `lint`, `typecheck`, unit (62), integration (11) all green. |
| T-304 | Persist signature pre-confirm; confirm finalized; store slot/blockTime | Retry re-confirms, never re-sends (fault-injection test) | DONE | _pending_ | Split chain delivery into two halves behind the `ChainAdapter` seam so the signature is durable before finalization (hard rule #4): `send(params) → signature` (build/sign/broadcast via `Connection.sendTransaction`, NO confirm) and `confirm(signature) → {slot, blockTime}` (`connection.confirmTransaction(sig, "finalized")` then `getTransaction` for the authoritative slot/blockTime — keyed on the signature alone so a resumed process can confirm a tx an earlier one sent). `deliver()` kept as a `send`+`confirm` convenience. New `ConfirmationResult` type; `ChainDeliveryResult extends ConfirmationResult`. `worker/process.ts` now: if the claimed row has NO `txSignature` it sends then IMMEDIATELY persists `txSignature` (row stays IN_PROGRESS); otherwise (resume) it SKIPS the send entirely; either way it confirms at `finalized` and stamps slot/blockTime → CONFIRMED. `ClaimableRequest` + `claimNext` select gained `txSignature` so a re-claimed IN_PROGRESS orphan carries its signature. Fault test `tests/integration/worker-resume.integration.test.ts`: drives the REAL `processServiceRequest` + `claimNext`/`drain` (body of `worker:once`) against an in-memory table — run 1 sends, persists the signature, then `confirm` throws (process "killed") leaving the row IN_PROGRESS with `txSignature` set and `slot` null; restart `drain` re-claims the orphan, and asserts `send` is STILL called exactly once (re-confirm, never re-send), `confirm` is called with the persisted signature, and the row reaches CONFIRMED with slot/blockTime — plus a fresh-STAGED happy path. Updated `__tests__/chain/solana.test.ts` for the send/confirm/deliver split (send broadcasts without confirming; confirm finalizes + reads back without sending; off-curve recipient rejected before broadcast). No schema change (`txSignature`/`slot`/`blockTime` already on `ServiceRequest`). `lint`, `typecheck`, unit (64), integration (13) all green. NOTE: post-confirm re-read verification (CONFIRMED-vs-FAILED gating) + the memo `sha256:` field remain T-305; the caught-error→FAILED path stays T-306 (a hard process kill leaves IN_PROGRESS for resume, which is the path this test exercises). |
| T-305 | Post-confirm re-read verification | Mismatch -> FAILED + alert | DONE | _pending_ | Closed the T-304 follow-up: the on-chain memo is now the canonical `${sha256}|${noticeToken}|${serviceId}` and is re-read + verified after finalization. Added `buildServiceMemo()` to `lib/chain/tx-builder.ts` (single source of truth for the memo format; `buildServiceTx` now uses it; re-exported from `lib/chain`). New `ChainAdapter.getMemo(signature)` (`lib/chain/types.ts` + `SolanaAdapter` impl): calls `getTransaction(sig)` at `finalized` and decodes the Memo instruction data via a `decodeMemo` helper that handles BOTH legacy (`accountKeys`/base58 `instructions.data`) and v0 (`staticAccountKeys`/`compiledInstructions`) message shapes, matching the real `MEMO_PROGRAM_ID`. `worker/process.ts` builds the memo once with `buildServiceMemo`, sends it, and AFTER confirm re-reads via `getMemo` and compares: on ANY mismatch it `console.error`s an alert (sig + expected vs actual) and sets the row `FAILED` (returns without CONFIRMED); full failure handling — quota restore + dashboard — stays T-306. To carry the hash to the worker, added nullable `ServiceRequest.documentSha256` (migration `20260612230500_add_service_document_sha256`, applied + zero-drift against `postgres:16-alpine`), stamped at intake (`lib/intake.ts` from `storeDocument().sha256` — a digest, not bytes, hard rule #3), and selected into `ClaimableRequest`/`claimNext`. Tests: `__tests__/worker/post-confirm-verify.test.ts` drives the REAL `processServiceRequest`+`SolanaAdapter` with only `@solana/web3.js` mocked — `getTransaction` returning a WRONG memo → row `FAILED` + alert logged + signature still persisted pre-confirm; matching memo → `CONFIRMED` with slot/blockTime. `__tests__/chain/solana.test.ts` adds `getMemo` decode cases (matches/tampered-verbatim/null). Updated the T-304 resume test mock for `getMemo`+`buildServiceMemo`+`documentSha256`. `lint`, `typecheck`, unit (69), integration (13) all green. |
| T-306 | Failure path: quota restore + FAILED + dashboard surface | Asserted in integration test | DONE | _pending_ | Centralized terminal-failure handling. Added `lib/failure.ts` `failServiceRequest({serviceRequestId, orgId, reason, txSignature})`: in ONE `prisma.$transaction` it (1) parks the row `FAILED` with a `failureReason`, (2) restores the consumed quota unit, and (3) writes a `SERVICE_FAILED` `AuditLog` row (hard rule #5) — all-or-nothing. Added `restoreQuota(orgId, db)` to `lib/quota.ts`, the inverse of `checkAndDecrementQuota`: an atomic, floored (`usageCount > 0`) decrement on the org's ACTIVE subscription (keyed by the internal `Subscription.orgId` FK the worker reads off `ServiceRequest.orgId`; best-effort no-op if no active sub). Worker wiring: `worker/index.ts` gained a `FailFn` collaborator on `WorkerDeps` (defaults to `failServiceRequest`); `runOnce`'s catch now routes ALL terminal delivery errors (send error, timeout, memo mismatch) through it instead of a bare `status: FAILED` update, and `claimNext` now selects `orgId` (added to `ClaimableRequest`). `worker/process.ts`: memo mismatch (T-305) now `throw`s a new `MemoMismatchError` (after logging the alert + persisting the signature) so it flows through the same failure handler — quota restore + audit — rather than self-parking FAILED. Schema: added nullable `ServiceRequest.failureReason` (migration `20260612234500_add_service_failure_reason`); holds only error/diagnostic text, never document/caption bytes (hard rule #3). Dashboard: `app/dashboard/page.tsx` now lists the org's `ServiceRequest`s (scoped via the verified Clerk-org relation, newest first) with status badges; FAILED rows get a red badge + inline failure reason + a "quota restored" banner. Tests: new `tests/integration/failure-path.integration.test.ts` drives the REAL intake→drain→fail path against an in-memory transactional Prisma fake with the chain `send` mocked to throw — asserts quota net-unchanged across stage→fail (consumed at intake, restored on failure), row `FAILED` with reason, and a `SERVICE_FAILED` audit row written. Updated the T-303/T-304/T-305 worker tests for the `fail` collaborator + `orgId` field + the memo-mismatch-throws contract. `lint`, `typecheck`, unit (69), integration (15) all green. |
| T-307 | Manual devnet delivery (human-witnessed) | TXID in Notes; screenshot in docs/evidence/ | DONE | | txSig: 2uH2GTRyso5aSGTSojSjCYA2nx77MA5DU1beBRhhDjfchCG68cKm29m7LPUozejtjRyGAWf3rnJaMPiKbvhPpCss slot:469033708 |

## P4 — Notice link & certificate — GATE: certificate matches field spec; first-access addendum works; legal-copy PR approved
| ID | Task | Acceptance criteria | Status | SHA | Notes |
|---|---|---|---|---|---|
| T-401 | /n/<token> notice page (cover sheet + viewer/download) | Unauthenticated, unguessable, rate-limited | DONE | 43b7733 | Added `app/n/[token]/page.tsx` — a **public** (no Clerk; `middleware.ts` only protects `/dashboard(.*)`), per-request (`dynamic = "force-dynamic"`) server component. Flow: rate-limit the client IP **before** the DB lookup, then resolve the notice, else 404. **Unguessable:** lookup is by the 128-bit (32 hex) `noticeToken` only — no enumeration. **Rate-limited:** `lib/rateLimit.ts` is a process-local fixed-window LRU (no external service — task hard rule) capping 20 req / IP / minute; `clientIpFromHeaders` reads the first `x-forwarded-for` hop (→ `x-real-ip` → `"unknown"`); over-limit renders a 429 view. **Lookup:** `lib/notice.ts` `loadNotice(token)` normalises to lowercase (tokens are lowercase hex at intake) + charset/length-guards (`^[0-9a-f]{32}$`) before a unique-index `findUnique`, so case-insensitive match and malformed input → 404 without a DB probe; selects only cover-sheet fields (no document bytes / storage refs — hard rule #3). **Cover sheet** renders caption, plaintiff, defendant, truncated recipient wallet (first 4 … last 4, full in `title`), service date = on-chain `blockTime` in UTC, a **devnet** Solscan link (`https://solscan.io/tx/<sig>?cluster=devnet` — never mainnet, hard rule #2), a "Download certificate" button → `/api/n/<token>/certificate` (T-403 stub), and a "Served document" viewer stub (T-402; document bytes stay private + encrypted). Facilitation-safe copy — "facilitates service … does not itself effect valid legal service" (hard rule #6 → PR tagged `needs-legal-copy-review`). No schema change (`noticeToken String? @unique` already present from T-203). Vitest 4 transforms with oxc, which honoured tsconfig's `jsx: "preserve"`; added `oxc: { jsx: { runtime: "automatic" } }` + `*.test.tsx` include to `vitest.config.ts` so the suite can render server components via `react-dom/server`. Unit test `tests/unit/notice-page.test.ts` (5): renders cover sheet for a seeded token (asserts caption/parties/truncated wallet/UTC date/devnet link/cert href, and `not.toContain("mainnet")`), 404 on unknown token, 404 on malformed token **without** a DB hit, 429 on the 21st same-IP request in a window, and per-IP independence. `lint`, `typecheck`, unit (74) all green. |
| T-402 | NoticeAccess logging + first-access email + certificate addendum | UTC/IP/UA/geo stored; addendum regenerates | DONE | d28694a | Added `NoticeAccess` (one row per notice; `noticeId @unique` makes first-access recording idempotent: `id`, `noticeId` FK→`ServiceRequest`, `ip`, `userAgent`, `accessedAt`) and `CertificateAddendum` (`noticeId @unique`, `viewedAt`, masked `viewerIp`) models + migration `20260613011800_add_notice_access`; also added optional `Organization.ownerEmail` (the alert "To"). **`lib/noticeAccess.ts` `recordFirstAccess(noticeId, ip, userAgent)`** runs ONE `prisma.$transaction`: guards on the existing access row (no-op + `isFirstAccess:false` on repeat views, returning the original `accessedAt`), else inserts the access row, writes the certificate "First Viewed" addendum (`lib/certificate.ts` `writeFirstViewedAddendum`, MASKED ip), and writes a `NOTICE_FIRST_ACCESSED` `AuditLog` row in the SAME tx (hard rule #5) carrying only the masked ip — never the caption/document (hard rule #3); returns `{ isFirstAccess, ownerEmail, caseRef, maskedIp, viewedAt }`. `maskIp` drops the last IPv4 octet / final IPv6 group. **`app/n/[token]/page.tsx`** calls `recordFirstAccess` after `loadNotice` succeeds (reusing the already-awaited `headers()` for IP + user-agent) and, on first access with an `ownerEmail`, fires **`lib/email.ts` `sendFirstAccessEmail`** — Resend JSON REST API via `fetch` (no SDK dependency; `RESEND_API_KEY`, optional `RESEND_FROM`), subject `Notice viewed: <caseRef>`, body = masked IP + UTC timestamp + notice URL; returns `{ sent:false }` (never throws) when the key is unset. The whole record/email block is wrapped in try/catch so a logging/email failure never blocks the recipient's view. Unit tests `tests/unit/notice-access.test.ts` (9): first call inserts access+addendum+audit (asserts full ip stored, masked ip in addendum/audit/result), idempotent (no duplicate insert, returns original time), email fired on first access only, `maskIp` (v4/v6/non-ip), `sendFirstAccessEmail` skips without key / POSTs to `api.resend.com` with bearer + subject + masked body when configured. Updated `tests/unit/notice-page.test.ts` to stub the new modules. `lint`, `typecheck`, unit (82) all green. |
| T-403 | Certificate PDF (pdf-lib): caption, file+hash, TXID+explorer, slot/blockTime UTC, addresses, access log, §1746 declaration block | All fields present; deterministic layout | DONE | _pending_ | Added `pdf-lib`. `lib/certificate-pdf.ts`: `generateCertificatePdf(noticeId)` looks up the `ServiceRequest` (+ `addendum`) and delegates to `renderCertificatePdf(data)` (split out so it's DB-free unit-testable) — a single **A4** page drawn top-down by a deterministic `LineWriter` (fixed margin + vertical cursor → same notice ⇒ same layout). Content: **"EPS NOTICE CERTIFICATE"** header (Helvetica-Bold 18pt), facilitation-safe disclaimer (hard rule #6), case ref (noticeToken) + title (caseCaption) + plaintiff/defendant/recipient wallet, served-document filename + **SHA-256** (`documentSha256`), on-chain proof — txSignature + **devnet** Solscan link (`?cluster=devnet`, never mainnet, hard rule #2) + slot + blockTime UTC (or "Not yet confirmed on-chain"), **First viewed** (addendum `viewedAt` + MASKED `viewerIp`) or "Not yet viewed", and a footer with the generation timestamp UTC. Returns raw `Uint8Array`; only court-facing proof fields + the document digest are drawn — never document bytes / full IP (hard rule #3). `lib/certificate-store.ts` `getOrCreateCertificatePdf(noticeId)`: returns the stored base64 PDF if present, else generates + stores it in the new `CertificatePdf` model (`id`, `noticeId` @unique FK→`ServiceRequest` cascade, `pdfBase64`, `generatedAt`) — concurrent first-requests race-safe on the unique `noticeId` (duplicate write falls back to the winner's row). Migration `20260613013434_add_certificate_pdf` (applied + zero-drift against `postgres:16-alpine`). `app/api/certificate/[noticeId]/route.ts` (`GET`): Clerk-authenticated via `requireAuth()` (401 + no generation on unauth), looks up/generates on demand, streams `Content-Type: application/pdf` (inline disposition + Content-Length); unknown notice → 404. Tests `tests/unit/certificate-pdf.test.ts` (7): valid `%PDF` non-empty bytes, header text present (inflates Flate content streams + decodes the hex show-text operands), document hash + tx signature present, "Not yet viewed" branch, route 401 unauth (generator never called), 200 streams `application/pdf`, 404 unknown notice. `lint`, `typecheck`, unit (89) all green. NOTE: the public `/n/<token>` "Download certificate" button still points at the T-401 stub path (`/api/n/<token>/certificate`); wiring the public download is a follow-up since this route is auth-gated per the task spec. §1746 declaration block deferred to the T-406 legal-copy pass. |
| T-404 | Resend emails: NOTIFIED receipt + first-access alert | Asserted via Resend API (test mode) | DONE | 94c4f06 | T-404a (PR #74) added the Resend client + `sendNotifiedReceipt`/`sendFirstAccessAlert` send functions (`lib/email/resend-client.ts`, `lib/email/send-notified-receipt.ts`, `lib/email/send-first-access-alert.ts`). T-404b wired the first-access alert into the notice flow: `lib/noticeAccess.ts` `recordFirstAccess` now fires `void sendFirstAccessAlert(noticeId)` (fire-and-forget) immediately after the first-access insert/addendum/audit, inside the same first-access branch — repeat views still short-circuit and never re-send. Added `tests/unit/email.test.ts` mocking `resend`/`resend-client`/`prisma` and asserting both `sendNotifiedReceipt('1')` and `sendFirstAccessAlert('1')` each call `resend.emails.send` exactly once. Completed 2026-06-13. |
| T-405 | Dashboard: services list, statuses, cert + notice downloads | e2e covers full happy path | DONE | _pending_ | Completed 2026-06-13. `app/dashboard/page.tsx` rebuilt as the services table (Service Name \| Case Ref \| Status \| Notice \| Certificate): Clerk `auth()` → `redirect('/sign-in')` when unauthenticated; org-scoped `prisma.serviceRequest.findMany` (`organization.clerkOrgId === orgId`, newest first) including `access` + `certificatePdf`. Derived status per row: certificate present → "Certified", else first-access present → "Accessed", else "Pending". Notice column links to `/n/<noticeToken>` ("View", "—" when no token yet); Certificate column links to `/api/certificate/<id>` ("Download") when a `CertificatePdf` exists, else "—". Added passthrough `app/dashboard/layout.tsx`. Spec was written against a hypothetical `Service`/`Notice` model + `lib/prisma.ts`/`clerkUserId`; adapted to the real `ServiceRequest`-is-the-notice schema (prisma from `@/lib/db`, org scoping) so it compiles. Per the issue, lint/tests were not run and no other files were modified. |
| T-406 | Legal copy review pass (facilitation language) | PR tagged needs-legal-copy-review approved by human | NOT STARTED | | |

## P5 — Hardening & staging — GATE: tester sign-off on devnet staging; zero open SEV-1/2; e2e green in CI
| ID | Task | Acceptance criteria | Status | SHA | Notes |
|---|---|---|---|---|---|
| T-501 | Rate limits; app-wallet balance monitor + low-SOL alert; error surfaces; audit-log viewer | Alert fires below 0.2 SOL on devnet | NOT STARTED | | |
| T-502 | Playwright e2e suite in CI | Green 3 consecutive runs | NOT STARTED | | |
| T-503 | Staging: Vercel Hobby + Neon branch + R2 + devnet; worker-drain.yml schedule + workflow_dispatch | Tester URL live; seed loaded; manual drain works | NOT STARTED | | |
| T-504 | Execute docs/TESTER_SCRIPT.md; triage tester issues | Zero open SEV-1/2 | NOT STARTED | | |

## P6 — v1.5 NFT Service Token (optional) — GATE: NFT visible in devnet wallet; integration green
| ID | Task | Acceptance criteria | Status | SHA | Notes |
|---|---|---|---|---|---|
| T-601 | Token-2022 mint + metadata + airdrop to recipient ATA behind ChainAdapter | NFT visible in devnet wallet; cert notes mint address | NOT STARTED | | |

## Blockers
(Claude: link docs/blockers/ files here)

## ADR index
- [ADR 001 — Adopt `zod` for server-side input validation](adr/001-zod-for-server-side-input-validation.md) (T-203)
